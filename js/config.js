/* ============================================================
   config.js — paste your Apps Script Web App URL below
   ------------------------------------------------------------
   Until you set this, the app runs in DEMO MODE using only
   the built-in sample roster and localStorage. The app fully
   works in demo mode — useful for showing it to the board
   before you set up the backend.

   Once you've followed backend/SETUP.md and deployed the
   Apps Script Web App, paste the deployment URL between the
   quotes below. The app will switch to live mode automatically.

   The URL looks like:
     https://script.google.com/macros/s/AK....../exec
   ============================================================ */

window.FF_CONFIG = {
  // Empty string = demo mode (localStorage only).
  // Paste your URL here when ready: e.g. "https://script.google.com/macros/s/AK..../exec"
  apiUrl: "https://script.google.com/macros/s/AKfycbwJV09A9NtK19nkWBhD-f_uBaea2CMhi_JWK63BZ34O_Ume7py9CMtoRt53BMX8jmuR0g/exec",

  // Whether self-signup is allowed. Disable if your conference is closed-roster.
  allowSelfSignup: true,
};
